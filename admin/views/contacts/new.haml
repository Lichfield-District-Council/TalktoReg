.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:contacts, :index)
      %li.active=link_to pat(:new), url(:contacts, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:contacts)
    .inner
      -form_for :contacts, url(:contacts, :create), :class => :form do |f|
        =partial "contacts/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
